# راهنمای یادگیری سیستم توصیه محصولات

<div dir="rtl">

این راهنما برای توسعه‌دهندگان مبتدی طراحی شده که می‌خواهند سیستم توصیه محصولات را بفهمند و یاد بگیرند.

</div>

---

## 📚 فهرست مطالب

1. [مقدمه: سیستم‌های توصیه چیست؟](#مقدمه)
2. [مفاهیم پایه Machine Learning](#مفاهیم-پایه)
3. [انواع سیستم‌های توصیه](#انواع-سیستم-های-توصیه)
4. [آموزش گام به گام پروژه](#آموزش-گام-به-گام)
5. [درک کدها](#درک-کدها)
6. [مثال‌های عملی](#مثال-های-عملی)
7. [سوالات متداول](#سوالات-متداول)

---

## 🎯 مقدمه: سیستم‌های توصیه چیست؟

### تعریف ساده

سیستم توصیه (Recommendation System) یک نرم‌افزار است که **پیشنهاد می‌دهد** کدام محصولات یا محتوا برای یک کاربر مناسب‌تر است.

### مثال‌های روزمره

1. **Netflix:** فیلم‌هایی که ممکن است دوست داشته باشید
2. **Amazon:** محصولاتی که دیگران با شما خریده‌اند
3. **Spotify:** موزیک‌های مشابه آهنگ فعلی شما
4. **Instagram:** پست‌هایی که احتمالاً دوست دارید

### چرا مهم است؟

- ✅ کاربران راحت‌تر محصول مورد نظر را پیدا می‌کنند
- ✅ فروش افزایش می‌یابد
- ✅ تجربه کاربری بهتر می‌شود
- ✅ رضایت کاربر بیشتر می‌شود

---

## 🧠 مفاهیم پایه Machine Learning

### Machine Learning چیست؟

**تعریف ساده:** ماشین یاد می‌گیرد از داده‌های گذشته برای پیش‌بینی آینده.

### مثال با انسان:

```
یک کودک:
1. می‌بیند: "گربه = پشم دار + میو میو"
2. یاد می‌گیرد: "پشم دار + میو میو = گربه"
3. بعداً تشخیص می‌دهد: "این یک گربه است!"
```

**در Machine Learning:**
```
سیستم:
1. می‌بیند: "کاربر A محصول X و Y را خریده"
2. یاد می‌گیرد: "کاربران مشابه محصول Z را هم می‌خرند"
3. پیشنهاد می‌دهد: "به کاربر B محصول Z را پیشنهاد بده"
```

### انواع یادگیری:

#### 1. Supervised Learning (یادگیری با نظارت)

**مثال:** معلم به دانش‌آموز می‌گوید "این یک سگ است" یا "این یک گربه است"

**در سیستم توصیه:**
- داده‌های قبلی داریم: "کاربر X محصول Y را خریده"
- سیستم یاد می‌گیرد: "کاربران مشابه چه چیزهایی می‌خرند"

#### 2. Unsupervised Learning (یادگیری بدون نظارت)

**مثال:** سیستم خودش الگوها را پیدا می‌کند

**در سیستم توصیه:**
- سیستم خودش کاربران مشابه را پیدا می‌کند
- خودش محصولات مشابه را پیدا می‌کند

---

## 🔍 انواع سیستم‌های توصیه

### 1. Collaborative Filtering (فیلتر همکاری‌محور)

**ایده اصلی:** "کاربرانی که مثل شما هستند، چه چیزهایی دوست دارند؟"

#### مثال واقعی:

```
علی:  [محصول 1: ⭐⭐⭐⭐, محصول 2: ⭐⭐⭐⭐⭐]
حسن:  [محصول 1: ⭐⭐⭐⭐, محصول 3: ⭐⭐⭐]
رضا:  [محصول 2: ⭐⭐⭐⭐⭐, محصول 3: ⭐⭐⭐⭐]

سیستم تشخیص می‌دهد:
- علی و حسن شبیه هستند (هر دو محصول 1 را دوست دارند)
- علی محصول 2 را دوست دارد
- پس احتمالاً حسن هم محصول 2 را دوست دارد
- سیستم به حسن محصول 2 را پیشنهاد می‌دهد
```

#### مزایا:
- ✅ نیاز به اطلاعات محصول ندارد
- ✅ خودش الگوها را پیدا می‌کند

#### معایب:
- ⚠️ برای کاربران جدید مشکل دارد (Cold Start Problem)
- ⚠️ اگر محصول جدید باشد، نمی‌تواند پیشنهاد بدهد

---

### 2. Content-Based Filtering (فیلتر محتوا محور)

**ایده اصلی:** "محصولاتی که شبیه محصولاتی هستند که شما دوست دارید"

#### مثال واقعی:

```
کاربر قبلاً خریده:
- لپتاپ Asus (قیمت: 20M, CPU: i7, RAM: 16GB)
- لپتاپ HP (قیمت: 18M, CPU: i5, RAM: 8GB)

ویژگی‌های مورد علاقه کاربر:
- قیمت: 15-25M
- CPU: قوی
- RAM: 8GB+

سیستم پیشنهاد می‌دهد:
- لپتاپ Dell (قیمت: 22M, CPU: i7, RAM: 16GB) ✅
- تبلت (قیمت: 5M) ❌ (خیلی ارزان)
```

#### مزایا:
- ✅ برای محصولات جدید کار می‌کند
- ✅ نیاز به کاربران مشابه ندارد

#### معایب:
- ⚠️ فقط محصولات مشابه پیشنهاد می‌دهد (تنوع ندارد)
- ⚠️ نیاز به اطلاعات محصول دارد

---

### 3. Hybrid Recommender (ترکیبی) ⭐

**ایده اصلی:** ترکیب Collaborative + Content-Based

**در این پروژه:**
- 60% Collaborative Filtering
- 40% Content-Based Filtering

#### چرا ترکیبی بهتر است؟

```
مثال:
کاربر A محصولات الکترونیکی می‌خرد.

Collaborative می‌گوید: "کاربران مشابه موبایل سامسونگ خریده‌اند"
Content-Based می‌گوید: "شما قبلاً گوشی خریدید، پس موبایل دیگر پیشنهاد بدیم"

ترکیبی می‌گوید:
- موبایل سامسونگ: امتیاز 0.6 × 0.8 + 0.4 × 0.9 = 0.84 ✅
- تبلت: امتیاز 0.6 × 0.5 + 0.4 × 0.7 = 0.58 ❌

نتیجه: موبایل سامسونگ پیشنهاد می‌شود!
```

---

## 📖 آموزش گام به گام پروژه

### مرحله 1: جمع‌آوری داده (Data Collection)

#### چه داده‌هایی نیاز داریم؟

```
1. اطلاعات کاربران (Users)
   - ID, Email, Name

2. اطلاعات محصولات (Products)
   - ID, Title, Price, Category, Stock

3. تاریخچه خرید (Orders)
   - کدام کاربر چه محصولی خریده
   - چه زمانی خریده
   - چقدر پول داده
```

#### در کد:

```python
# object_loader.py

def load_users() -> List[User]:
    """بارگذاری کاربران از دیتابیس"""
    # SQL query: SELECT * FROM users
    # تبدیل به لیست User objects
    return users

def load_products() -> List[Product]:
    """بارگذاری محصولات از دیتابیس"""
    # SQL query: SELECT * FROM products WHERE status = 1
    return products
```

**مثال واقعی:**
```
Users: 224,959 کاربر
Products: 36,114 محصول فعال
Orders: 80,737 سفارش
```

---

### مرحله 2: تبدیل داده به تعامل (Interactions)

#### تعامل چیست؟

**تعریف:** هر نوع ارتباط کاربر با محصول

```
مثال‌ها:
- خرید (purchase): امتیاز 5.0
- اضافه به سبد (cart_add): امتیاز 3.0
- مشاهده (view): امتیاز 1.0
- علاقه‌مندی (wishlist): امتیاز 4.0
```

#### در کد:

```python
# generate_recommendations.py

def create_user_product_interactions(order_items_df):
    """تبدیل سفارشات به تعاملات"""
    interactions = []
    
    for order in order_items:
        interaction = ProductInteraction(
            user_id=order.user_id,
            product_id=order.product_id,
            interaction_type='purchase',  # نوع: خرید
            timestamp=order.created_at,
            value=order.total_price  # ارزش: قیمت خرید
        )
        interactions.append(interaction)
    
    return interactions
```

**مثال:**
```
کاربر 123 محصول 456 را به قیمت 100,000 تومان خریده

interaction = {
    user_id: 123,
    product_id: 456,
    interaction_type: 'purchase',
    value: 100000
}
```

---

### مرحله 3: آموزش مدل Collaborative Filtering

#### قدم 1: ساخت ماتریس کاربر-محصول

**ماتریس چیست؟**

```
ماتریس = جدول دو بعدی

            محصول 1   محصول 2   محصول 3
کاربر 1       5         3         0
کاربر 2       4         0         5
کاربر 3       0         4         3
```

**اعداد یعنی چه؟**
- 5 = کاربر محصول را خریده (امتیاز بالا)
- 3 = کاربر محصول را دیده یا به سبد اضافه کرده
- 0 = کاربر هیچ تعاملی نداشته

#### در کد:

```python
# collaborative_filtering.py

def build_user_item_matrix(self, interactions):
    """ساخت ماتریس کاربر-محصول"""
    
    # گام 1: محاسبه امتیاز برای هر تعامل
    for interaction in interactions:
        if interaction.type == 'purchase':
            score = 5.0 + (interaction.value / 100000)  # خرید = امتیاز بالا
        elif interaction.type == 'view':
            score = 1.0  # فقط دیده = امتیاز پایین
    
    # گام 2: ساخت ماتریس
    # کاربر 1 → محصول 1: score
    # کاربر 1 → محصول 2: score
    # ...
```

**مثال واقعی:**
```
کاربر 9194445:
- محصول 12345 خریده (100K) → score = 6.0
- محصول 67890 دیده → score = 1.0

ماتریس[9194445][12345] = 6.0
ماتریس[9194445][67890] = 1.0
```

#### قدم 2: محاسبه شباهت کاربران

**شباهت چیست؟**

**مثال ساده:**
```
علی:  [1, 2, 3]  # 3 فیلم را دوست دارد
حسن:  [1, 2, 4]  # 2 فیلم مشترک با علی
رضا:  [5, 6, 7]  # هیچ فیلم مشترکی ندارد

شباهت علی و حسن: بالا ✅
شباهت علی و رضا: صفر ❌
```

**فرمول Cosine Similarity:**

```
شباهت = (تعداد محصولات مشترک) / (جذر مجموع مربعات هر دو)

مثال:
کاربر A: [1, 1, 0, 1]  (3 محصول)
کاربر B: [1, 1, 1, 0]  (3 محصول)
محصولات مشترک: [1, 1] = 2

شباهت = 2 / (√3 × √3) = 2/3 = 0.67
```

#### در کد:

```python
# collaborative_filtering.py

def calculate_user_similarities(self):
    """محاسبه شباهت بین کاربران"""
    
    similarities = []
    
    for user1 in all_users:
        for user2 in all_users:
            if user1 != user2:
                # محاسبه Cosine Similarity
                similarity = cosine_similarity(
                    user1_vector,  # ردیف کاربر 1 در ماتریس
                    user2_vector   # ردیف کاربر 2 در ماتریس
                )
                similarities.append((user1, user2, similarity))
    
    return similarities
```

---

### مرحله 4: تولید توصیه‌ها (Collaborative)

#### فرآیند:

```
1. پیدا کردن کاربران مشابه
2. پیدا کردن محصولاتی که کاربران مشابه خریده‌اند
3. محاسبه امتیاز برای هر محصول
4. مرتب‌سازی بر اساس امتیاز
```

#### در کد:

```python
# collaborative_filtering.py

def get_user_recommendations(self, user_id, top_k=10):
    """تولید توصیه برای یک کاربر"""
    
    # گام 1: پیدا کردن کاربران مشابه
    similar_users = self.get_similar_users(user_id, top_k=10)
    # نتیجه: [(user_456, 0.8), (user_789, 0.7), ...]
    
    # گام 2: پیدا کردن محصولاتی که کاربران مشابه خریده‌اند
    recommendations = {}
    
    for similar_user, similarity in similar_users:
        # محصولاتی که کاربر مشابه خریده اما کاربر فعلی نه
        products = get_products_user_bought(similar_user)
        
        for product_id in products:
            if product_id not in user_own_products:
                # محاسبه امتیاز
                score = similarity * product_rating
                recommendations[product_id] += score
    
    # گام 3: مرتب‌سازی
    sorted_recommendations = sorted(
        recommendations.items(), 
        key=lambda x: x[1], 
        reverse=True
    )
    
    return sorted_recommendations[:top_k]
```

**مثال واقعی:**

```
کاربر 9194445:
- کاربران مشابه: [9391201 (0.09%), ...]
- محصول 501838 را کاربران مشابه خریده‌اند
- امتیاز = 0.0009 × 5.0 = 1587.0
- نتیجه: "محصول 501838 پیشنهاد می‌شود با امتیاز 1587.0"
```

---

### مرحله 5: آموزش مدل Content-Based

#### قدم 1: استخراج ویژگی‌های محصول

**ویژگی چیست؟**

```
مثال لپتاپ:
- قیمت: 20,000,000 تومان
- CPU: Intel i7
- RAM: 16GB
- Storage: 512GB SSD
- Brand: Asus
```

#### در کد:

```python
# content_based_filtering.py

def extract_product_features(self, products):
    """استخراج ویژگی‌های محصولات"""
    
    features = {}
    
    for product in products:
        # ویژگی‌های متنی
        text = f"{product.title} {product.slug} {product.sku}".lower()
        
        # ویژگی‌های عددی
        price_range = self._get_price_range(product.price)
        # "low", "medium", "high", "premium"
        
        stock_level = self._get_stock_level(product.stock)
        # "low", "medium", "high"
        
        features[product.id] = {
            'text': text,
            'price_range': price_range,
            'category_id': product.category_id,
            'price': product.price,
            'stock': product.stock
        }
    
    return features
```

**مثال:**
```
محصول 12345:
{
    'text': 'لپتاپ asus rog zephyrus',
    'price_range': 'high',
    'category_id': 10,
    'price': 25000000,
    'stock': 5
}
```

#### قدم 2: ساخت پروفایل کاربر

**پروفایل کاربر چیست؟**

```
مجموعه ویژگی‌های محصولاتی که کاربر دوست دارد یا خریده

مثال:
کاربر خریده:
- لپتاپ (قیمت: 20M, دسته: الکترونیک)
- موبایل (قیمت: 15M, دسته: الکترونیک)
- هدفون (قیمت: 2M, دسته: الکترونیک)

پروفایل کاربر:
- دسته‌بندی مورد علاقه: الکترونیک (3 بار)
- محدوده قیمت: 2M - 20M
- علاقه به لوازم الکترونیک: بالا
```

#### در کد:

```python
# content_based_filtering.py

def build_user_profile(self, user_id, user_interactions, products):
    """ساخت پروفایل کاربر"""
    
    profile = {
        'preferred_categories': {},
        'price_range': [],
        'preferred_features': []
    }
    
    # بررسی محصولاتی که کاربر خریده
    for interaction in user_interactions:
        product = products[interaction.product_id]
        
        # دسته‌بندی مورد علاقه
        if product.category_id:
            profile['preferred_categories'][product.category_id] += 1
        
        # محدوده قیمت
        profile['price_range'].append(product.price)
    
    # محاسبه میانگین
    profile['average_price'] = sum(profile['price_range']) / len(...)
    
    return profile
```

#### قدم 3: محاسبه شباهت محصول

**چگونه دو محصول را با هم مقایسه کنیم؟**

```
فرمول TF-IDF + Cosine Similarity:

1. استخراج کلمات مهم از عنوان/توضیحات
2. محاسبه وزن هر کلمه (TF-IDF)
3. تبدیل به بردار (Vector)
4. محاسبه Cosine Similarity
```

**مثال:**

```
محصول 1: "لپتاپ Asus ROG Gaming"
محصول 2: "لپتاپ Asus ZenBook"
محصول 3: "موبایل Samsung Galaxy"

TF-IDF:
- "لپتاپ": در هر دو محصول 1 و 2 وجود دارد → مشابه
- "Gaming": فقط در محصول 1 → متفاوت
- "ZenBook": فقط در محصول 2 → متفاوت

نتیجه:
- شباهت محصول 1 و 2: 0.7 ✅ (هر دو لپتاپ Asus)
- شباهت محصول 1 و 3: 0.1 ❌ (یکی لپتاپ یکی موبایل)
```

#### در کد:

```python
# content_based_filtering.py

def calculate_product_similarities(self, products):
    """محاسبه شباهت بین محصولات"""
    
    # گام 1: تبدیل متن به بردار (TF-IDF)
    vectorizer = TfidfVectorizer()
    product_texts = [p['text'] for p in products]
    vectors = vectorizer.fit_transform(product_texts)
    
    # گام 2: محاسبه Cosine Similarity
    similarities = cosine_similarity(vectors)
    
    # نتیجه: ماتریس N×N
    # similarities[i][j] = شباهت محصول i و j
    return similarities
```

---

### مرحله 6: تولید توصیه‌ها (Content-Based)

#### فرآیند:

```
1. ساخت پروفایل کاربر از خریدهای قبلی
2. پیدا کردن محصولات مشابه محصولات قبلی
3. محاسبه امتیاز بر اساس شباهت
4. مرتب‌سازی
```

#### در کد:

```python
# content_based_filtering.py

def get_user_recommendations(self, user_id, top_k=10):
    """تولید توصیه Content-Based"""
    
    # گام 1: دریافت پروفایل کاربر
    user_profile = self.user_profiles[user_id]
    
    # گام 2: پیدا کردن محصولاتی که کاربر خریده
    user_products = get_products_user_bought(user_id)
    
    recommendations = {}
    
    for bought_product_id in user_products:
        # پیدا کردن محصولات مشابه
        similar_products = self.get_similar_products(
            bought_product_id, 
            top_k=5
        )
        
        for similar_product_id, similarity in similar_products:
            if similar_product_id not in user_own_products:
                # محاسبه امتیاز
                score = similarity * confidence
                recommendations[similar_product_id] += score
    
    # گام 3: مرتب‌سازی
    return sorted(recommendations.items(), key=lambda x: x[1], reverse=True)
```

---

### مرحله 7: ترکیب توصیه‌ها (Hybrid)

#### چرا ترکیب؟

```
Collaborative می‌گوید: "محصول X را پیشنهاد بده" (امتیاز: 1000)
Content-Based می‌گوید: "محصول X را پیشنهاد بده" (امتیاز: 800)

ترکیبی:
امتیاز نهایی = 0.6 × 1000 + 0.4 × 800 = 600 + 320 = 920

قدرتمندتر از هر کدام به تنهایی! ✅
```

#### در کد:

```python
# hybrid_recommender.py

def get_recommendations(self, user_id, top_k=10):
    """تولید توصیه‌های ترکیبی"""
    
    recommendations = []
    
    # گام 1: دریافت توصیه‌های Collaborative
    if self.collaborative_model:
        collab_recs = self.collaborative_model.get_recommendations(
            user_id, top_k * 2
        )
        # اعمال وزن 60%
        for rec in collab_recs:
            rec.score *= 0.6  # collaborative_weight
        recommendations.extend(collab_recs)
    
    # گام 2: دریافت توصیه‌های Content-Based
    if self.content_model:
        content_recs = self.content_model.get_user_recommendations(
            user_id, top_k * 2
        )
        # اعمال وزن 40%
        for rec in content_recs:
            rec.score *= 0.4  # content_weight
        recommendations.extend(content_recs)
    
    # گام 3: ترکیب و رتبه‌بندی
    final_recommendations = self._combine_recommendations(
        recommendations, top_k
    )
    
    return final_recommendations
```

**مثال واقعی:**

```
کاربر 9194445:

Collaborative:
- محصول 501838: امتیاز 1587 × 0.6 = 952.2

Content-Based:
- محصول 501838: امتیاز 800 × 0.4 = 320

ترکیب:
- محصول 501838: 952.2 + 320 = 1272.2 ✅

نتیجه: محصول 501838 با امتیاز 1272.2 پیشنهاد می‌شود
```

---

### مرحله 8: اعمال فیلترهای تجاری

#### فیلتر چیست؟

**تعریف:** حذف محصولاتی که نمی‌توانیم نمایش دهیم

#### فیلترهای ما:

```
1. موجودی محصول:
   - اگر موجودی = 0 → حذف ❌
   - فقط محصولات با موجودی > 0 → نمایش ✅

2. وضعیت محصول:
   - اگر status != 'published' → حذف ❌
   - فقط محصولات منتشر شده → نمایش ✅

3. خریدهای قبلی:
   - اگر کاربر قبلاً خریده → حذف ❌
   - فقط محصولات جدید → نمایش ✅
```

#### در کد:

```python
# hybrid_recommender.py

def _apply_business_filters(self, recommendations, user_id):
    """اعمال فیلترهای تجاری"""
    
    filtered = []
    
    for rec in recommendations:
        product = self.products_dict.get(rec.product_id)
        
        # فیلتر 1: موجودی
        if product.stock_quantity <= 0:
            continue  # حذف ❌
        
        # فیلتر 2: وضعیت
        if product.status != 'published':
            continue  # حذف ❌
        
        # فیلتر 3: خریدهای قبلی
        user_purchases = self.user_interactions.get(user_id, [])
        if rec.product_id in [p.product_id for p in user_purchases]:
            continue  # حذف ❌ (کاربر قبلاً خریده)
        
        # اگر همه فیلترها را پاس کرد → اضافه کردن ✅
        filtered.append(rec)
    
    return filtered
```

---

### مرحله 9: ذخیره در Redis

#### چرا Redis؟

```
Redis = یک دیتابیس در حافظه (In-Memory Database)

مزایا:
- خیلی سریع: < 1ms ✅
- مناسب برای Caching ✅
- ساختار ساده: Key-Value ✅

مقایسه:
- Database: ~50-100ms ❌
- Redis: < 1ms ✅
- فایل CSV: ~10-20ms ❌
```

#### ساختار ذخیره‌سازی:

```
کلید (Key):
  recommendation:9194445

مقدار (Value):
  [
    {
      "product_id": 501838,
      "score": 1587.0,
      "reason": "Collaborative: 1 کاربران مشابه...",
      "confidence": 1.0,
      "collaborative_details": "{...}"
    },
    {
      "product_id": 123456,
      "score": 1200.0,
      ...
    }
  ]
```

#### در کد:

```python
# recommendation_storage.py

def store_recommendations(self, user_id, recommendations):
    """ذخیره توصیه‌ها در Redis"""
    
    key = f"recommendation:{user_id}"
    
    # تبدیل به JSON
    data = json.dumps(recommendations, ensure_ascii=False)
    
    # ذخیره با TTL (7 روز)
    self.client.setex(
        key,
        ttl=604800,  # 7 روز
        value=data
    )
```

---

### مرحله 10: دریافت از API

#### فرآیند:

```
کاربر → API Request → Redis → پاسخ سریع (< 1ms)
                ↓ (اگر وجود نداشت)
              Fallback → تولید مستقیم (~50ms)
```

#### در کد:

```python
# recommendation_api.py

@app.get("/recommendations/{user_id}")
async def get_user_recommendations(user_id: int, limit: int = 10):
    """دریافت توصیه‌ها از API"""
    
    # گام 1: تلاش برای خواندن از Redis
    storage = get_storage()
    if storage.exists(user_id):
        recs = storage.get_recommendations(user_id)
        return recs[:limit]  # سریع! < 1ms ✅
    
    # گام 2: Fallback - تولید مستقیم
    recommender = get_recommender()
    recommendations = recommender.get_recommendations(user_id, limit)
    return recommendations  # کندتر ~50ms ⚠️
```

---

## 💻 درک کدها

### ساختار کلی پروژه:

```
1. داده‌ها (Data)
   ├── object_loader.py      # بارگذاری Users, Products
   ├── dataframe_loader.py   # بارگذاری به صورت DataFrame
   └── models.py             # تعریف ساختار داده (User, Product, Order)

2. پردازش (Processing)
   ├── collaborative_filtering.py    # الگوریتم Collaborative
   ├── content_based_filtering.py    # الگوریتم Content-Based
   └── hybrid_recommender.py         # ترکیب دو الگوریتم

3. ذخیره‌سازی (Storage)
   └── recommendation_storage.py    # مدیریت Redis

4. API
   └── recommendation_api.py        # FastAPI endpoints

5. اجرا (Execution)
   ├── generate_recommendations.py    # تولید توصیه‌ها
   └── run_recommendation.py         # CLI tool
```

---

### توضیح فایل‌های کلیدی:

#### 1. `models.py` - ساختار داده

```python
@dataclass
class User:
    """مدل کاربر"""
    id: int              # شناسه یکتا
    email: str           # ایمیل
    name: str            # نام
    created_at: datetime # تاریخ ثبت‌نام

@dataclass
class Product:
    """مدل محصول"""
    id: int              # شناسه یکتا
    title: str           # عنوان
    price: float         # قیمت
    stock_quantity: int  # موجودی
    status: str          # وضعیت (published/draft)

@dataclass
class Recommendation:
    """مدل توصیه"""
    user_id: int         # کاربری که برایش توصیه شده
    product_id: int      # محصول پیشنهادی
    score: float         # امتیاز (هرچه بالاتر بهتر)
    reason: str          # دلیل توصیه
    confidence: float    # میزان اطمینان (0-1)
```

**چرا مهم است؟**
- ساختار داده را مشخص می‌کند
- کد خوانا می‌شود
- خطاها راحت‌تر پیدا می‌شوند

---

#### 2. `collaborative_filtering.py` - الگوریتم Collaborative

```python
class CollaborativeFiltering:
    """سیستم توصیه مبتنی بر همکاری کاربران"""
    
    def build_user_item_matrix(self, interactions):
        """
        ساخت ماتریس کاربر-محصول
        
        مثال:
        کاربر 1 → محصول 1: 5.0
        کاربر 1 → محصول 2: 3.0
        کاربر 2 → محصول 1: 4.0
        """
        # کد...
    
    def calculate_user_similarities(self):
        """
        محاسبه شباهت بین کاربران
        
        استفاده از Cosine Similarity:
        شباهت = (محصولات مشترک) / (جذر مجموع مربعات)
        """
        # کد...
    
    def get_user_recommendations(self, user_id, top_k):
        """
        تولید توصیه برای کاربر
        
        1. پیدا کردن کاربران مشابه
        2. پیدا کردن محصولات آن‌ها
        3. محاسبه امتیاز
        4. مرتب‌سازی
        """
        # کد...
```

**مثال عملی:**

```python
# فرض کنید:
users = [1, 2, 3]
products = [10, 20, 30]

# ماتریس:
#         محصول 10   محصول 20   محصول 30
# کاربر 1    5         3         0
# کاربر 2    4         0         5
# کاربر 3    0         4         3

# شباهت کاربر 1 و 2:
# محصولات مشترک: فقط محصول 10 (هر دو خریده‌اند)
# شباهت ≈ 0.6

# برای کاربر 1:
# - کاربر 2 مشابه است (0.6)
# - کاربر 2 محصول 30 را خریده (5.0)
# - امتیاز پیشنهاد = 0.6 × 5.0 = 3.0

# نتیجه: محصول 30 به کاربر 1 پیشنهاد می‌شود
```

---

#### 3. `content_based_filtering.py` - الگوریتم Content-Based

```python
class ContentBasedFiltering:
    """سیستم توصیه مبتنی بر محتوا"""
    
    def extract_product_features(self, products):
        """
        استخراج ویژگی‌های محصول
        
        مثال:
        - متن: "لپتاپ asus rog gaming"
        - قیمت: "high" (بالای 1M)
        - دسته: "الکترونیک"
        """
        # کد...
    
    def build_user_profile(self, user_id, interactions):
        """
        ساخت پروفایل کاربر
        
        از خریدهای قبلی:
        - دسته‌بندی‌های مورد علاقه
        - محدوده قیمت
        - برندهای مورد علاقه
        """
        # کد...
    
    def calculate_product_similarities(self, products):
        """
        محاسبه شباهت بین محصولات
        
        استفاده از TF-IDF + Cosine Similarity:
        - تبدیل متن به بردار
        - محاسبه شباهت
        """
        # کد...
```

**مثال عملی:**

```python
# محصولات:
product_1 = {
    'title': 'لپتاپ Asus ROG Gaming',
    'price': 25000000,
    'category': 'الکترونیک'
}

product_2 = {
    'title': 'لپتاپ Asus ZenBook',
    'price': 20000000,
    'category': 'الکترونیک'
}

product_3 = {
    'title': 'موبایل Samsung Galaxy',
    'price': 15000000,
    'category': 'الکترونیک'
}

# شباهت:
similarity_1_2 = 0.7  # هر دو لپتاپ Asus
similarity_1_3 = 0.2  # یکی لپتاپ یکی موبایل

# کاربر قبلاً product_1 را خریده
# سیستم پیشنهاد می‌دهد: product_2 (شباهت بالا)
```

---

#### 4. `hybrid_recommender.py` - ترکیب دو الگوریتم

```python
class HybridRecommender:
    """سیستم توصیه ترکیبی"""
    
    def __init__(self):
        self.collaborative_weight = 0.6  # 60%
        self.content_weight = 0.4        # 40%
    
    def get_recommendations(self, user_id, top_k):
        """
        ترکیب توصیه‌های Collaborative و Content-Based
        
        فرآیند:
        1. دریافت توصیه‌های Collaborative (× 0.6)
        2. دریافت توصیه‌های Content-Based (× 0.4)
        3. ترکیب و رتبه‌بندی
        4. اعمال فیلترهای تجاری
        """
        
        # Collaborative
        collab_recs = self.collaborative_model.get_recommendations(...)
        for rec in collab_recs:
            rec.score *= 0.6
        
        # Content-Based
        content_recs = self.content_model.get_recommendations(...)
        for rec in content_recs:
            rec.score *= 0.4
        
        # ترکیب
        all_recs = collab_recs + content_recs
        combined = self._combine_recommendations(all_recs)
        
        # فیلتر
        filtered = self._apply_business_filters(combined, user_id)
        
        return filtered[:top_k]
```

**مثال عملی:**

```
کاربر 9194445:

Collaborative پیشنهاد می‌دهد:
- محصول 501838: امتیاز 1587 × 0.6 = 952.2

Content-Based پیشنهاد می‌دهد:
- محصول 501838: امتیاز 800 × 0.4 = 320.0
- محصول 123456: امتیاز 600 × 0.4 = 240.0

ترکیب:
- محصول 501838: 952.2 + 320.0 = 1272.2 ✅ (بالاترین)
- محصول 123456: 0 + 240.0 = 240.0

نتیجه: محصول 501838 به عنوان بهترین پیشنهاد
```

---

#### 5. `generate_recommendations.py` - تولید توصیه‌ها

```python
def main():
    """
    تابع اصلی: تولید توصیه برای همه کاربران
    
    فرآیند:
    1. بارگذاری داده‌ها
    2. آموزش مدل‌ها
    3. تولید توصیه برای هر کاربر
    4. ذخیره در فایل و Redis
    """
    
    # گام 1: بارگذاری
    users = load_users_from_db()      # 224,959 کاربر
    products = load_products_from_db() # 36,114 محصول
    orders = load_order_items(...)    # 80,737 سفارش
    
    # گام 2: تبدیل به تعامل
    interactions = create_user_product_interactions(orders)
    
    # گام 3: آموزش
    recommender = HybridRecommender()
    recommender.train()
    
    # گام 4: تولید توصیه برای هر کاربر
    for user in users:
        recommendations = recommender.get_recommendations(
            user.id, 
            top_k=20
        )
        # ذخیره...
```

**مثال واقعی:**

```
برای کاربر 9194445:

1. سیستم Collaborative می‌گوید:
   - کاربران مشابه: [9391201, ...]
   - محصولات آن‌ها: [501838, ...]
   - امتیاز Collaborative: 1587.0

2. سیستم Content-Based می‌گوید:
   - محصولات مشابه خریدهای قبلی: [501838, ...]
   - امتیاز Content-Based: 800.0

3. ترکیب:
   - امتیاز نهایی: 0.6 × 1587 + 0.4 × 800 = 1272.2

4. فیلتر:
   - موجودی دارد؟ ✅
   - منتشر شده؟ ✅
   - کاربر قبلاً نخریده؟ ✅

5. نتیجه:
   - محصول 501838 با امتیاز 1272.2 پیشنهاد می‌شود ✅
```

---

## 🎓 مثال‌های عملی

### مثال 1: فرآیند کامل برای یک کاربر

```
کاربر جدید: ID = 999999

مرحله 1: بررسی خریدهای قبلی
- کاربر 999999 چه خریده؟
  → محصول 12345 (لپتاپ)
  → محصول 67890 (موبایل)

مرحله 2: Collaborative Filtering
- پیدا کردن کاربران مشابه:
  کاربر 111111: [12345, 67890, 11111] ← مشابه!
  کاربر 222222: [12345, 67890, 22222] ← مشابه!
- محصولات مشترک:
  → محصول 11111 (هدفون) - کاربران مشابه خریده‌اند
  → محصول 22222 (کیبورد) - کاربران مشابه خریده‌اند

مرحله 3: Content-Based Filtering
- ویژگی‌های خریدهای قبلی:
  - دسته: الکترونیک
  - قیمت: 10M - 30M
- محصولات مشابه:
  → محصول 33333 (لپتاپ دیگر) - مشابه محصول 12345
  → محصول 44444 (موبایل دیگر) - مشابه محصول 67890

مرحله 4: ترکیب
- محصول 11111: 0.6 × 800 + 0.4 × 200 = 560 ✅
- محصول 22222: 0.6 × 700 + 0.4 × 150 = 480
- محصول 33333: 0.6 × 100 + 0.4 × 900 = 420
- محصول 44444: 0.6 × 50 + 0.4 × 850 = 370

مرحله 5: فیلتر
- محصول 11111: موجودی دارد ✅ → نمایش ✅
- محصول 22222: موجودی ندارد ❌ → حذف ❌

نتیجه نهایی:
1. محصول 11111 (امتیاز: 560)
2. محصول 33333 (امتیاز: 420)
3. محصول 44444 (امتیاز: 370)
```

---

### مثال 2: محاسبه شباهت (تشریحی)

#### Cosine Similarity - گام به گام

```
کاربر A خریده: [محصول 1, محصول 2]
کاربر B خریده: [محصول 1, محصول 3]

گام 1: ساخت بردار
کاربر A: [1, 1, 0]  # محصول 1 ✓, محصول 2 ✓, محصول 3 ✗
کاربر B: [1, 0, 1]  # محصول 1 ✓, محصول 2 ✗, محصول 3 ✓

گام 2: محاسبه Dot Product
A · B = (1×1) + (1×0) + (0×1) = 1 + 0 + 0 = 1

گام 3: محاسبه Magnitude
|A| = √(1² + 1² + 0²) = √2 ≈ 1.41
|B| = √(1² + 0² + 1²) = √2 ≈ 1.41

گام 4: Cosine Similarity
similarity = (A · B) / (|A| × |B|)
          = 1 / (1.41 × 1.41)
          = 1 / 2
          = 0.5

نتیجه: شباهت کاربر A و B = 50%
```

---

## 🔢 مفاهیم ریاضی (ساده‌شده)

### 1. Cosine Similarity (شباهت کسینوس)

**تعریف ساده:** اندازه‌گیری اینکه دو بردار چقدر در یک جهت هستند

```
مثال:
بردار A: →
بردار B: →
شباهت = زاویه بین دو بردار (هرچه کوچکتر، مشابه‌تر)
```

**فرمول:**

```
similarity = (A · B) / (|A| × |B|)

که در آن:
- A · B = حاصل ضرب داخلی (تعداد مشترکات)
- |A| = اندازه بردار A
- |B| = اندازه بردار B
```

**مثال عددی:**

```
کاربر 1: [1, 1, 0, 1]  (3 محصول)
کاربر 2: [1, 1, 1, 0]  (3 محصول)

A · B = 1×1 + 1×1 + 0×1 + 1×0 = 2
|A| = √(1²+1²+0²+1²) = √3 ≈ 1.73
|B| = √(1²+1²+1²+0²) = √3 ≈ 1.73

similarity = 2 / (1.73 × 1.73) = 2/3 ≈ 0.67
```

---

### 2. TF-IDF (Term Frequency-Inverse Document Frequency)

**تعریف ساده:** وزن دادن به کلمات بر اساس اهمیت

```
TF (Term Frequency): تعداد تکرار کلمه در سند
IDF (Inverse Document Frequency): نادر بودن کلمه در کل اسناد

مثال:
سند 1: "لپتاپ Asus Gaming"
سند 2: "لپتاپ Asus ZenBook"
سند 3: "موبایل Samsung"

TF-IDF برای "لپتاپ":
- TF در سند 1: 1 (یک بار آمده)
- IDF: log(3/2) ≈ 0.4 (در 2 از 3 سند آمده)
- TF-IDF: 1 × 0.4 = 0.4

TF-IDF برای "Gaming":
- TF در سند 1: 1
- IDF: log(3/1) ≈ 1.1 (فقط در 1 سند آمده - خاص!)
- TF-IDF: 1 × 1.1 = 1.1 (بالاتر - مهم‌تر!)
```

---

### 3. Matrix (ماتریس)

**تعریف ساده:** جدول دو بعدی اعداد

```
مثال ماتریس کاربر-محصول:

        محصول 1   محصول 2   محصول 3
کاربر 1    5         3         0
کاربر 2    4         0         5
کاربر 3    0         4         3

معنی:
- کاربر 1 محصول 1 را خریده (امتیاز: 5)
- کاربر 1 محصول 2 را دیده (امتیاز: 3)
- کاربر 1 محصول 3 را ندیده (امتیاز: 0)
```

---

## ❓ سوالات متداول

### Q1: چرا Hybrid بهتر از هر کدام به تنهایی است؟

**جواب:**

```
Collaborative: "کاربران مشابه چی خریده‌اند؟"
→ خوب برای پیدا کردن محصولات محبوب
→ بد برای محصولات جدید (Cold Start)

Content-Based: "محصولات مشابه چی هستند؟"
→ خوب برای محصولات جدید
→ بد برای تنوع (فقط مشابه پیشنهاد می‌دهد)

Hybrid: ترکیب هر دو
→ بهترین از هر دو دنیا ✅
→ قدرت Collaborative + تنوع Content-Based
```

---

### Q2: چرا از Redis استفاده می‌کنیم؟

**جواب:**

```
مشکل:
- تولید توصیه برای هر کاربر: ~50-100ms
- اگر 1000 کاربر همزمان درخواست کنند: خیلی کند! ❌

راه حل:
- یک بار تولید کنیم (15-45 دقیقه)
- در Redis ذخیره کنیم
- خواندن: < 1ms ⚡

مقایسه:
Database: 50-100ms
File CSV: 10-20ms
Redis: < 1ms ✅ (100 برابر سریع‌تر!)
```

---

### Q3: امتیاز (Score) یعنی چی؟

**جواب:**

```
امتیاز = میزان مناسب بودن محصول برای کاربر

مثال:
محصول 1: امتیاز 1587.0 ✅ (خیلی مناسب)
محصول 2: امتیاز 500.0  (متوسط)
محصول 3: امتیاز 50.0   ❌ (کم مناسب)

هرچه بالاتر = بهتر = در رتبه بالاتر نمایش می‌دهیم
```

---

### Q4: چرا 20 توصیه برای هر کاربر؟

**جواب:**

```
دلایل:

1. UX بهتر:
   - کاربر گزینه بیشتری دارد
   - احتمال پیدا کردن محصول مورد نظر بیشتر

2. فیلتر شدن:
   - بعد از فیلترها (موجودی، خرید قبلی) ممکن است 10-15 تا باقی بماند
   - بهتر است 20 تا داشته باشیم

3. عملکرد:
   - تولید 20 تا فقط کمی بیشتر از 10 تا زمان می‌برد
   - ارزش آن را دارد ✅
```

---

### Q5: چرا TTL 7 روز در Redis؟

**جواب:**

```
دلایل:

1. داده‌ها تغییر می‌کنند:
   - محصولات جدید اضافه می‌شوند
   - موجودی تغییر می‌کند
   - کاربران خرید می‌کنند

2. کیفیت:
   - توصیه‌های قدیمی‌تر = دقت کمتر
   - 7 روز = تعادل بین کیفیت و عملکرد

3. حافظه:
   - جلوگیری از پر شدن Redis
   - حذف خودکار توصیه‌های قدیمی
```

---

### Q6: Cold Start Problem چیست؟

**جواب:**

```
مشکل: کاربر جدید هیچ خرید قبلی ندارد

Collaborative نمی‌تواند:
- کاربران مشابه را پیدا کند (چون خرید نداشته)
- توصیه بدهد ❌

Content-Based نمی‌تواند:
- پروفایل کاربر را بسازد (چون خرید نداشته)
- توصیه بدهد ❌

راه حل:
1. توصیه محصولات محبوب (Popular Products)
2. توصیه محصولات جدید (New Products)
3. استفاده از داده‌های دموگرافیک (سن، جنسیت، ...)
```

---

## 📝 خلاصه فرآیند کامل

```
┌─────────────────────────────────────────┐
│ 1. بارگذاری داده‌ها                     │
│    - کاربران (224,959)                  │
│    - محصولات (36,114)                   │
│    - سفارشات (80,737)                   │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 2. تبدیل به تعاملات                     │
│    - خرید → امتیاز 5.0                  │
│    - مشاهده → امتیاز 1.0                │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 3. آموزش Collaborative                  │
│    - ساخت ماتریس کاربر-محصول            │
│    - محاسبه شباهت کاربران               │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 4. آموزش Content-Based                  │
│    - استخراج ویژگی محصولات             │
│    - ساخت پروفایل کاربر                 │
│    - محاسبه شباهت محصولات               │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 5. تولید توصیه برای هر کاربر           │
│    - Collaborative (60%)                │
│    - Content-Based (40%)                 │
│    - ترکیب                               │
│    - فیلتر                               │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 6. ذخیره                                │
│    - فایل CSV/Parquet                   │
│    - Redis (با TTL 7 روز)                │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 7. API                                  │
│    - دریافت از Redis (< 1ms)            │
│    - یا تولید مستقیم (~50ms)            │
└─────────────────────────────────────────┘
```

---

## 🎯 تمرین‌های عملی

### تمرین 1: درک ماتریس

```
1. فرض کنید 3 کاربر و 3 محصول داریم
2. ماتریس زیر را پر کنید:

        محصول A   محصول B   محصول C
کاربر 1    ?         ?         ?
کاربر 2    ?         ?         ?
کاربر 3    ?         ?         ?

اطلاعات:
- کاربر 1: محصول A و B را خریده
- کاربر 2: محصول A و C را خریده
- کاربر 3: محصول B را دیده (نخریده)
```

---

### تمرین 2: محاسبه شباهت

```
کاربر A: [محصول 1, محصول 2]
کاربر B: [محصول 1, محصول 3]
کاربر C: [محصول 2, محصول 3]

1. شباهت A و B را محاسبه کنید
2. شباهت A و C را محاسبه کنید
3. کدام دو کاربر بیشتر مشابه هستند؟
```

---

### تمرین 3: تولید توصیه

```
فرض کنید:
- کاربر X خریده: [لپتاپ Asus, موبایل Samsung]
- کاربر Y خریده: [لپتاپ Asus, موبایل Samsung, هدفون Sony]
- کاربر Z خریده: [تبلت iPad, هدفون Sony]

1. کدام دو کاربر مشابه هستند؟
2. به کاربر X چه محصولی پیشنهاد می‌دهید؟ چرا؟
```

---

## 📚 منابع بیشتر

### مفاهیم Machine Learning:

1. **Supervised vs Unsupervised Learning**
   - [Coursera: Machine Learning Course](https://www.coursera.org/learn/machine-learning)

2. **Cosine Similarity**
   - فرمول ریاضی و کاربردها

3. **TF-IDF**
   - استخراج ویژگی از متن

### سیستم‌های توصیه:

1. **Netflix Prize**
   - مسابقه‌ای که Collaborative Filtering را محبوب کرد

2. **Matrix Factorization**
   - روش پیشرفته‌تر Collaborative Filtering

---

## 🎓 مراحل یادگیری پیشنهادی

### سطح 1: مبتدی (شما الان اینجا هستید)

- ✅ فهمیدن مفاهیم پایه
- ✅ درک فرآیند کلی
- ✅ خواندن و درک کدهای ساده

### سطح 2: متوسط

- 🔄 تغییر وزن‌ها (Collaborative vs Content-Based)
- 🔄 اضافه کردن ویژگی‌های جدید
- 🔄 تست با داده‌های مختلف

### سطح 3: پیشرفته

- 🔄 پیاده‌سازی الگوریتم‌های جدید
- 🔄 بهینه‌سازی عملکرد
- 🔄 A/B Testing

---

## 💡 نکات مهم

1. **صبر داشته باشید:** Machine Learning زمان می‌برد
2. **تست کنید:** همیشه با نمونه کوچک شروع کنید
3. **مستندات بخوانید:** کدها را با دقت مطالعه کنید
4. **سوال بپرسید:** اگر چیزی نفهمیدید، بپرسید
5. **تمرین کنید:** با مثال‌های کوچک شروع کنید

---

**موفق باشید در یادگیری! 🚀**

اگر سوالی دارید یا می‌خواهید جزئیات بیشتری بفهمید، به [GUIDE.md](GUIDE.md) مراجعه کنید.


